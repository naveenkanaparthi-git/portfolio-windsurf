import { generateMetadata } from "@/lib/metadata";

export const metadata = generateMetadata({
  title: "Projects",
  description: "Data engineering projects showcasing pipeline development, real-time analytics, and scalable architectures.",
  url: "/projects"
});
